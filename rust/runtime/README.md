# quadra-a-runtime

Runtime helpers for local Rust applications built on `quadra-a`.

This crate sits above `quadra-a-core` and provides local runtime surfaces such as relay/session coordination, inbox handling, and daemon-adjacent runtime helpers.

## Install

```bash
cargo add quadra-a-runtime
```

## Development

```bash
cargo test -p quadra-a-runtime
```
